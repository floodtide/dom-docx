# Local-only strategy / research (not open-sourced)
internal/

node_modules/
dist/
output/**/*
!output/**/.gitkeep
playwright-report/
test-results/
.DS_Store
# LibreOffice lock files (created when a .docx/.odt is open, e.g. during the visual harness)
.~lock.*#
